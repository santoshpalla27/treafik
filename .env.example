# ============================================ 
# TRAEFIK & PORTAINER ENVIRONMENT VARIABLES 
# ============================================ 

# --- Domain Configuration --- 
TRAEFIK_DOMAIN=traefik.yourdomain.com 
PORTAINER_DOMAIN=portainer.yourdomain.com 
ROOT_DOMAIN=yourdomain.com

# --- Let's Encrypt Configuration --- 
LETSENCRYPT_EMAIL=your-email @example.com

# --- Basic Authentication --- 
# Generate hash using: docker run --rm httpd:alpine htpasswd -nbB admin 'YourPassword' | cut -d ":" -f 2 | sed 's/\\$/\\\\$$/g' 
BASIC_AUTH_USER=admin 
BASIC_AUTH_HASH=$$2y$$05$$REPLACE_WITH_YOUR_HASH

# --- Ports --- 
HTTP_PORT=80 
HTTPS_PORT=443

# --- Timezone --- 
TZ=UTC

# --- Cloudflare (Optional - for DNS challenge) --- 
# CF_API_EMAIL=your-email @example.com 
# CF_API_KEY=your-cloudflare-api-key